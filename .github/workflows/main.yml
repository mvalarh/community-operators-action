on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Simple test example
    steps:
    - uses: actions/checkout@v2
    - id: test
      uses:  mvalarh/community-operators-action@v1
      with:
        test-type: 'oragne'
        stream: 'upstream-community-operators'
        pf-labels: 'allow/operator-overwrite bug'
