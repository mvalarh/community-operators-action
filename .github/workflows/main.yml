on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Community operator simple test example
    steps:
    - uses: actions/checkout@v2
    - id: test
      uses: actions/hello-world-composite-run-steps-action@v1
      with:
        stream: 'upstream-community-operators'
